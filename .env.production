NEXT_PUBLIC_API_URL=https://api.sanganai.co.zw
BACKEND_URL=https://api.sanganai.co.zw
NEXT_PUBLIC_USE_MOCK=false
# Set this to a strong random secret in Vercel / your deployment dashboard.
# Never commit the real value to git.
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=REPLACE_WITH_STRONG_SECRET_IN_DEPLOYMENT_ENV
